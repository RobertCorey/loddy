# The 2020-era dependency tree has unsatisfiable peer ranges under npm 7+
# strictness (e.g. ng2-currency-mask pins Angular 9 peers). The tree is
# intentionally frozen — install it as npm 6 would have.
legacy-peer-deps=true
